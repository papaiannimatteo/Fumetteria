import { CommandUserCommand } from './command-user-command';

describe('CommandUserCommand', () => {
  it('should create an instance', () => {
    expect(new CommandUserCommand()).toBeTruthy();
  });
});
